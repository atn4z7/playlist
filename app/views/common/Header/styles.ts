import { StyleSheet } from 'react-native'
import { dimension } from 'styles'

const PADDING_VERTICAL = dimension.fullHeight * 0.05

export default StyleSheet.create({
  container: {
    paddingVertical: PADDING_VERTICAL
  }
})
